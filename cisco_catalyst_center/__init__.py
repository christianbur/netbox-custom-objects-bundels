"""Cisco Catalyst Center (netascode) data-model bundle.

A pure schema bundle: it ships only ``schema/catalyst_center.yaml`` (a 13-COT
portable schema modelling the Cisco Catalyst Center data model) and no COT
views. The package still needs to be importable for bundle discovery; importing
it has no side effects.
"""
