"""Cisco ACI (APIC) data-model bundle.

A pure schema bundle: it ships only ``schema/aci.yaml`` (a 17-COT portable
schema modelling the Cisco ACI / APIC tenant and fabric data model) and no COT
views. The package still needs to be importable for bundle discovery; importing
it has no side effects.
"""
