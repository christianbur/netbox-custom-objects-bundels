"""Cisco Meraki (netascode) data-model bundle.

A pure schema bundle: it ships only ``schema/meraki.yaml`` (a 9-COT portable
schema modelling the Cisco Meraki / netascode data model) and no COT views. The
package still needs to be importable for bundle discovery; importing it has no
side effects.
"""
